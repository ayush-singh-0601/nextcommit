# NextCommit

Give every repository a next step.

```bash
npx nextcommit scan . --json
```

NextCommit collects deterministic local repository signals. Use the bundled Codex skill to verify opportunities and generate implementation plans.
